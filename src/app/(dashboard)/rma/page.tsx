export default function Page() {
  return (
    <div>
      <h1 className="font-disp text-2xl font-bold capitalize">rma</h1>
      <p className="text-sm text-slate-500 mt-2 max-w-lg">
        This module's UI/data layer isn't wired up yet in this starter build.
        Dashboard, Orders, and IMEI Inventory are fully live against Neon —
        see DEPLOY.md for the recommended way to build out the rest.
      </p>
    </div>
  );
}
